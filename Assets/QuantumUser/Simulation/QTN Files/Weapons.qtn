component Weapon 
{
   FP CooldownTime;
   AssetRef<WeaponData> WeaponData;
}

component Bullet
{
    EntityRef Owner;
    FP Damage;
    FP Time;
    FP Speed;
    FPVector2 Direction;   
    FP HeightOffset;
}

signal CreateBullet(EntityRef Owner,Weapon Weapon);

event BulletHit
{
    EntityRef Bullet;
}