component Weapon 
{
   FP CooldownTime;
   byte Ammo;
   AssetRef<WeaponData> WeaponData;
}

component Bullet
{
    EntityRef Owner;
    FP Damage;
    FP Time;
    FP Speed;
    FPVector2 Direction;   
    FP HeightOffset;
}

signal CreateBullet(EntityRef Owner,AssetRef<FiringWeaponData> Weapon);

event BulletHit
{
    EntityRef Bullet;
}