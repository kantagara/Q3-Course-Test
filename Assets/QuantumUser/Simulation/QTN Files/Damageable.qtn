component Damageable{
    FP CurrentHealth;
    FP MaxHealth;
    AssetRef<DamageableBase> DamageableBase;
}

signal PlayerKilled(EntityRef Player);

synced event PlayerHealthUpdated{
    EntityRef Entity;
    FP CurrentHealth;
    FP MaxHealth;
}