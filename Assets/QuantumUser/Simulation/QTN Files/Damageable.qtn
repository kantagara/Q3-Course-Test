component Damageable{
    FP CurrentHealth;
    FP MaxHealth;
    AssetRef<DamageableBase> DamageableBase;
}

signal PlayerKilled(EntityRef Player);

event PlayerHealthUpdated{
    EntityRef Entity;
    FP CurrentHealth;
    FP MaxHealth;
}